module.exports = async options => {
  
  if ( typeof window !== 'undefined' ){

    const handleOnClient = require( './handleOnClient.js' )
    return await handleOnClient( options )
    
  } else {
    
    const handleOnServer = require( './handleOnServer.js' )
    return await handleOnServer( options )
    
  }
}